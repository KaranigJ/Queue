using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Random = UnityEngine.Random;

public class DebugDummyColor : MonoBehaviour
{
    /*private SpriteRenderer _spriteRenderer;

    private void Awake()
    {
        _spriteRenderer = GetComponent<SpriteRenderer>();
        _spriteRenderer.color = RandomColor();
    }

    private Color32 RandomColor()
    {
        byte min = 128;
        byte max = 255;

        byte r = (byte) Random.Range(min, max);
        byte g = (byte) Random.Range(min, max);
        byte b = (byte) Random.Range(min, max);

        var color = new Color32(r, g, b, 255);

        return color;
    }*/
}
